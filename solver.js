module.exports = Solver;

var Game = function(initialString) {

  this.cells = [];
  this.rows = [];
  this.cols = [];
  this.blocks = [];

  for (i = 0; i < initialString.length; i++) {
    cells.push(new Cell(initialString[i]));
  }



  var Cell = function(value) {
    this.value = value;
  };

  var Group = function(cells) {

  };

};
